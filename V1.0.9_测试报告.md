# PWA V1.0.9 测试报告

任务编号：`PWA-REMOTE-007`

## 结论

自动化测试：`PASS`

## 核心覆盖

- PENDING×2与EXECUTED/PENDING_NAV隔离：通过。
- 待执行数量2→1→0及列表即时刷新：通过。
- 计划100、实际100即时记账：通过。
- 计划100、实际150，手动追加50且有效期数仅增加1：通过。
- 现金池优先、外部资金仅记录不足部分：通过。
- T日NAV存在进入CONFIRMED并按高精度算法计算份额：通过。
- T日NAV不存在进入PENDING_NAV：通过。
- PENDING→NOT_EXECUTED，金额、本金、XIRR现金流、现金池、期数均不增加：通过。
- NOT_EXECUTED周期不会被补录扫描重新生成：通过。
- 确认执行和未执行均写入执行状态审计：通过。
- 历史EXECUTED记录不因新界面自动变化：通过。
- 区间收益率-3%、多次投入5%、策略退出、内部调拨边界：通过。
- 接管29天显示提示、30天不显示：通过。
- XIRR与区间收益率独立计算和显示：通过。

## 测试方式

- 策略、行情、校准、本金修订、基金份额核心测试：通过。
- 项目结构、版本、缓存、语法和静态资源检查：通过。
- 隔离浏览器真实IndexedDB V3→V4读取：通过。
- 隔离浏览器待执行闭环、NAV回填和人工校准：通过。
- JSON备份清空后恢复，关键Store计数一致：通过。
- Service Worker控制下断网重开：通过。
- Python行情更新脚本语法：通过。
- 凭据模式扫描：未发现风险。

所有测试使用隔离浏览器配置和测试数据库，未访问或修改正式用户IndexedDB。
