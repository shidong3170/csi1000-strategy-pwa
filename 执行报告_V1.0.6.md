# PWA-REMOTE-004 执行报告｜V1.0.6

## 执行口径

- 以正式工程 V1.0.5 的内容树为基线，在 `feature/realtime-index-close-state-v1.0.6` 分支增量实施。
- 本次仅修正完整收盘状态机、增加实时指数观察展示并同步版本资料。
- Frozen 策略规则、日K主数据口径、IndexedDB 结构和个人数据边界均未改变。

## Bug修复

- A股时点固定按 `Asia/Shanghai` 判断，不受设备或服务器时区影响。
- 开盘前及盘中以最近完整交易日收盘维持当前建议，状态为 `VALID_USING_LAST_COMPLETE_CLOSE`。
- 15:00后允许接纳当天完整日K；18:30前如未取得，状态为 `WAITING_TODAY_CLOSE`，保留已有建议。
- 18:30后仍缺当日收盘时标记 `MARKET_DATA_DELAYED`，只暂缓新的市场三因素加码；仓位、硬上限、现金池、XIRR和期限规则继续生效。
- 超过7个交易日按 `MARKET_DATA_STALE` 降级。
- T日完整收盘仍通过交易日历生成下一实际交易日建议，不使用自然日简单加一。

## 实时行情实现

- 新增东方财富中证1000实时/准实时直连接口，校验代码 `000852`、名称、点位、涨跌、涨跌幅和行情时间。
- 实时行情使用运行时 `realtime_quote` 对象；完整日K使用独立 `completed_daily_market` 数据集。
- 实时值不写入日K、IndexedDB行情集、MA200、回撤、近3年位置或三因素金额。
- Service Worker 不缓存外部实时与日K请求，避免旧响应被误当成最新行情。

## UI

- 分析页拆为“实时市场”和“策略计算基准”两张卡片。
- 首页同时显示完整收盘基准与实时指数，并明确实时指数不参与策略计算。
- 顶部版本和离线缓存版本升级为 V1.0.6。

## 时间

- 开始时间：2026-09-14 13:32:35 +08:00
- 开发与验收完成时间：2026-09-14 13:54:00 +08:00
