# PWA-REMOTE-004 测试结果｜V1.0.6

## 自动化回归

| 检查 | 结果 |
|---|---|
| 交易日开盘前、盘中沿用上一完整收盘 | 通过 |
| 15:00～18:30缺今日收盘显示等待并保留建议 | 通过 |
| 15:00后取得今日完整日K立即切换并生成下一交易日建议 | 通过 |
| 18:30后缺今日收盘标记延迟、市场加码暂缓 | 通过 |
| 周末与法定休市使用最近完整交易日 | 通过 |
| UTC输入仍按 Asia/Shanghai 判断 | 通过 |
| 实时成功/失败与日K成功/失败独立处理 | 通过 |
| 两路失败保留 LOCAL_CACHE | 通过 |
| 实时字段不会进入完整日K存储 | 通过 |
| >7交易日过期降级 | 通过 |
| Frozen 仓位、硬上限、年度增量、现金池、XIRR、止盈 | 通过 |
| 人工校准、基金市值依据、本金变化记录 | 通过 |
| PWA版本、Service Worker和静态结构 | 通过 |
| 代码格式冲突检查 | 通过 |

执行：

```text
node tests/market-data-core.test.js
node tests/strategy-core.test.js
node tests/calibration-core.test.js
node tests/check-project.js
node tests/browser-smoke.js
git diff --check
```

## 真实浏览器与接口联调

| 场景 | 结果 |
|---|---|
| HTTPS实时接口返回代码、名称、点位、涨跌、涨跌幅和时间 | 通过 |
| CORS响应允许浏览器读取 | 通过（`Access-Control-Allow-Origin: *`） |
| 浏览器实时成功 | 通过：显示 7436.42，来源 `MANUAL_DIRECT_REALTIME` |
| 正式收盘基准与实时值分离 | 通过：策略基准仍为 7427.24 |
| 外部接口波动 | 通过：实时卡提示不可用，日K回退 `GITHUB_AUTO`，策略未被清空 |
| 页面版本及两卡片结构 | 通过 |

浏览器控制连接组件不可用后，改用本机 Edge 无界面测试模式完成同一页面联调。测试使用独立浏览器资料目录与独立 localhost 数据库；临时资料已清理，不影响手机正式数据。

## 非阻断观察

- 联调期间东方财富连接出现过一次可达、一次波动；应用的实时失败隔离和日K三级回退均按设计生效。
