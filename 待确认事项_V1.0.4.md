# PWA-REMOTE-002 待确认事项｜V1.0.4

## 发布前

- 远程开发站当前访问 GitHub 时偶发连接重置；提交前重新核对 `origin/main` 与本地 V1.0.3 基线一致。

## 发布后

- 用手机正式 PWA 网络点击一次“更新行情”，确认来源为 `MANUAL_DIRECT`。远程开发站受当前出口网络影响只能验收到自动回退。
- 再做一次手机断网测试，确认来源为 `LOCAL_CACHE`，已有行情和私人数据均保留。
- 继续执行原迁移观察期，至少观察至 2026-09-17；本次升级不重置观察期。

## 非阻断说明

- 东方财富为第三方接口，未来仍可能调整 CORS 或可用性；`GITHUB_AUTO` 和 `LOCAL_CACHE` 是正式保底通道，不应删除。
- 无下一年度可靠交易日历时继续按 Frozen 规则降级，不猜测交易日。

