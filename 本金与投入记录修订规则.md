# 本金与投入记录修订规则｜V1.0.7

## 规则边界

事实记录允许修正，策略规则不允许回写历史。计划金额和当时策略版本是历史策略事实；实际金额、资金来源、执行状态、日期和备注是可核对修正的交易事实。

## 双状态

- 执行状态：`PENDING`（待执行）、`EXECUTED`（已执行）、`NOT_EXECUTED`（未执行）、`BLOCKED_HARD_LIMIT`（因硬上限停止）。
- 修订状态：`ORIGINAL`（原始）、`MODIFIED`（已修改）。
- 两个维度独立展示，例如“已执行 · 已修改”。

## 修订留痕

每个改变字段分别记录实体类型、实体编号、字段、原值、新值、修改时间、原因和来源。当前记录保存首次修改前的 `originalValues`，多次修改的全部日志均保留。

## 金额一致性

已执行记录必须满足：

```text
实际投入 = 现金池出资 + 外部资金
```

待执行、未执行和因硬上限停止记录的三项金额均为0，不计本金、期数、现金池或XIRR。现金池和在场本金不得因修订变为负数。

## 硬上限

待执行或因硬上限停止的记录改为已执行时，必须重新通过对应年度硬上限校验。已发生的历史事实修正优先保留；如果修正后的历史事实与策略上限冲突，记录审计标识，不篡改真实交易。

## 全链路重算

修订保存时在同一 IndexedDB 事务中更新当前记录、关联现金池流水、外部XIRR现金流和修订日志。页面随后自动重新计算当前在场本金、历史累计外部投入、有效期数、仓位、硬上限剩余、现金池、XIRR、收益状态和策略建议。

## 本金口径

- 历史累计外部投入 = 初始化历史累计投入本金 + 接管后真实外部新增资金；止盈不减少此数。
- 当前在场本金 = 接管日在场本金 + 接管后有效投入 + 现金池再投入 − 止盈退出对应本金。
- 现金池再投入增加基金风险暴露和当前在场本金，但不属于新的外部投入。

## 备份

V1.0.7 JSON备份结构版本为2，包含 `record_revisions`；恢复同时恢复修订历史，并兼容结构版本1的旧备份。
