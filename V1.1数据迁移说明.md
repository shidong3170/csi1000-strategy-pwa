# V1.1 Frozen 数据迁移说明

## 版本

- 来源：PWA V1.0.7 / IndexedDB V3 / JSON备份V2及更早兼容版本
- 目标：PWA V1.0.8 / IndexedDB V4 / JSON备份V3

## IndexedDB升级

升级过程只新增和扩展，不删除原有业务仓库或记录：

- 新增 `fund_profiles`、`fund_nav_daily`、`share_confirmation_events`。
- 为基金净值增加基金代码+净值日期唯一索引和日期索引。
- 为基金资料增加生效日期索引，为份额事件增加对象索引。
- 旧定投和手动买入补齐基金、净值、确认份额与份额状态字段。
- 旧的已执行投入在没有可靠原T日基金净值时迁移为 `PENDING_NAV`，不编造份额。
- 非已执行记录迁移为 `NOT_APPLICABLE`。
- 初始化与人工校准快照成为份额锚点；止盈自动快照不是人工校准锚点。
- 原本金、现金池、外部现金流、策略、修订、校准、市场行情和设置保持原值。

迁移不会使用000852指数反推基金净值或份额。升级完成后，用户需先显式配置实际基金，再按需补录历史T日正式单位净值。

## JSON备份兼容

- 新版导出结构版本为V3，并包含全部对象仓库。
- 导入V1/V2备份时自动建立新增空集合并补齐安全默认字段。
- 旧的已执行记录同样迁移为待净值确认；无法确定的份额保持空值。
- 校准锚点和原有修订日志保留。
- 若备份把000852写入基金资料或基金净值，导入会被拒绝。

## 回退说明

数据库完成V4升级后，不应再用V1.0.7程序直接打开同一数据库。需要回退程序时，应先保留V1.0.8完整JSON备份；旧程序无法理解新增基金净值与份额对象。
