# PWA-REMOTE-005 测试结果｜V1.0.7

## 自动化与业务回归

| 检查 | 结果 |
|---|---|
| 基线 21,380 → 21,180，期初外部现金流与本金同步修正 | 通过 |
| 定投 100 → 200、100 → 600，计划额不变且手动追加正确 | 通过 |
| 已执行 → 未执行；待执行 → 已执行 / 未执行 | 通过 |
| 因硬上限停止不入账且普通编辑不能绕过 | 通过 |
| 现金池 100 → 外部 100，本金不变且流水重建 | 通过 |
| 负现金池、负本金、资金恒等式和硬上限强校验 | 通过 |
| 历史事实超限保留并产生审计标记 | 通过 |
| 多次修改完整留存修订历史 | 通过 |
| 当前本金、历史外部投入、现金池、仓位和剩余额度勾稽 | 通过 |
| IndexedDB V3 刷新持久化 | 通过 |
| JSON备份所含修订历史恢复 | 通过 |
| V1.0.6实时市场与完整收盘双口径 | 通过 |
| T→T+1、三因素、15%～16%、18%/19%、年度+6000、现金池优先、止盈、XIRR | 通过 |
| 基金市值依据、人工校准、GitHub行情兜底 | 通过 |
| Service Worker V1.0.7 断网重载 | 通过 |
| 静态结构与格式冲突检查 | 通过 |

执行：

```text
node tests/principal-revision-core.test.js
node tests/calibration-core.test.js
node tests/strategy-core.test.js
node tests/market-data-core.test.js
node tests/check-project.js
node tests/browser-smoke.js
git diff --check
```

## 真实浏览器验收

- 页面顶部显示 `PWA V1.0.7`。
- 在隔离数据库中完成待执行计划、实际执行、投入金额修正、基线修正和修订历史恢复。
- 修正后本金为 21,380 元，计划 100 元保持不变，实际投入 200 元，记录同时显示“已执行 · 已修改”。
- 页面刷新后数据库版本、当前本金和 4 条字段修订保持一致。
- 模拟断网并重新加载页面后，V1.0.7 页面与本金数据仍可读取，Service Worker 已接管页面。
- 实时接口不可用时正确回退 GitHub 自动行情，完整收盘基准仍为 7427.24，策略不被清空。

浏览器测试使用独立本地资料目录和独立数据库，未触碰手机正式数据；验收后临时资料已清理。

