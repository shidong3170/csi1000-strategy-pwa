# PWA V1.0.9 实现差异与开发说明

任务编号：`PWA-REMOTE-007`

## 依据与基线

- 云端 Sources 快照 SHA256：全部通过。
- 总入口：V1.10.0。
- 软件开发规范：V1.0.2。
- PWA专题入口：V1.2.3。
- Frozen、数据模型、计算/状态机：V1.1。
- 正式软件基线：PWA V1.0.8，commit `3a03339e`。
- 工作分支：`feature/v1.0.9-pending-xirr-ux`。

## 实现内容

### 待执行入口

- 首页“待执行确认 N 期”的整条待办和按钮均可点击。
- 待执行视图严格筛选当前有效 `executionStatus === PENDING`，按日期全局倒序。
- `EXECUTED + PENDING_NAV` 不进入该视图。
- 显示日期、类型、原计划、当前计划、执行状态、修订状态、备注和操作。

### 待执行闭环

- 新增“确认已执行”和“未执行”主操作，保留“修正记录”为次级入口。
- 确认执行时默认实际金额等于当前计划，允许按真实成交修改。
- 资金来源继续使用现金池优先、外部资金补足算法。
- 同一事务写入记录、现金池流水、外部现金流、份额事件和执行状态审计。
- T日NAV存在时确认份额；不存在时进入 `PENDING_NAV`，不使用T+1、最近净值、000852或估值替代。
- 未执行写入 `NOT_EXECUTED + NOT_APPLICABLE`，实际金额为0，不增加本金、现金流或有效期数。

### 收益展示

新增正式接管后区间收益率：

`净外部投入 = -Σ现有有效 external_cashflows.amountCent`

其中现有现金流事实仅允许接管开账、外部投入和策略退出；基金与现金池内部调拨不进入外部现金流。

`正式期末资产 = 最近有效真实人工校准基金市值 + 当前现金池余额`

`区间收益率 = (正式期末资产 - 净外部投入) / 净外部投入`

若接管日、开账现金流、外部现金流、正式终值不完整，或净外部投入不大于0，则显示“暂不可计算”。接管不足30天时显示XIRR年化放大提示。正式与模拟XIRR算法均未修改。

## 数据与兼容性

- IndexedDB继续使用V4。
- JSON备份schema继续使用V3。
- 未新增Object Store，无业务数据迁移。
- V1.0.8既有记录按原事实读取，不自动改写。
- 云端 Sources 未修改。
