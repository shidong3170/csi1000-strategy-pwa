# V1.1 Frozen 开发变更清单

任务：PWA-REMOTE-006

软件版本：PWA V1.0.8

数据库版本：IndexedDB V4
备份结构版本：V3

## 数据与计算

- 新增 `fund_profiles`：基金代码、名称、净值来源、生效区间和版本历史；拒绝000852。
- 新增 `fund_nav_daily`：按基金代码和净值日期唯一保存六位定点单位净值，支持人工录入及修正。
- 新增 `share_confirmation_events`：保存份额确认与重新确认审计事件。
- 扩展定投、手动买入、赎回和校准快照的份额状态、确认份额、锚点及纠偏字段。
- 新增 `fund-share-core.js`，使用整数与 BigInt 完成净值、份额、市值和比例赎回的最终持久化计算。
- 已执行投入即时计入本金、资金来源、外部现金流和有效期数；缺少原T日净值仅延迟份额确认。
- 只允许使用同一基金的原T日正式净值确认份额；T+1、最近净值、000852和盘中估值均不能替代。
- 当前递推份额从最近真实人工校准锚点开始，加确认买入、减确认赎回，排除待确认和未执行记录。
- 人工校准保存校准前递推份额、纠偏和新锚点。
- 正式市值/XIRR继续使用最近真实校准；自动模拟市值/XIRR使用递推份额和最新有效基金净值。
- 修复日期加减受本地时区影响而可能仍返回原日期的问题，并统一以UTC日期运算处理纯日期。

## 界面与审计

- 首页、分析页和基金市值依据页分别显示真实校准与自动模拟口径。
- 单笔投入显示执行、修订、份额确认三个独立状态。
- 新增基金资料与基金净值管理页，支持净值修正原因和修订历史。
- 人工校准新增必填原因，并展示校准前份额和纠偏值。
- 自动基金净值适配器在没有可靠供应商时明确显示不可用，不伪造成功。

## 备份与PWA

- JSON备份覆盖全部18个对象仓库，兼容结构V1/V2/V3。
- V1/V2旧备份导入时补齐份额默认状态、校准锚点和新增空仓库。
- Service Worker缓存更新为 `csi1000-pwa-v1.0.8`，缓存新增计算模块。
- GitHub Pages工作流新增发布前Node回归门禁，验证通过后才部署。

## 测试

- 新增基金净值/份额纯计算测试与旧备份规范化测试。
- 浏览器回归新增真实IndexedDB V3→V4升级、T+1不替代、原T日回填、净值修正、正式/模拟口径、完整恢复和离线重载。
