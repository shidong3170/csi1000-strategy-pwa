# 中证1000定投PWA V1.1 Frozen 实现差异分析

## 1. 核验信息

- 任务：PWA-REMOTE-006
- 核验日期：2026-09-15（Asia/Shanghai）
- 正式项目：`中证1000定投PWA`
- 当前正式软件：PWA V1.0.7
- 正式源码基线：`677ac93de4234ad2bfd7236699da973df9335909`
- 工作分支：`feature/fund-nav-share-confirmation-v1.0.8`
- 云端现行规则链：六份要求文件均存在、版本匹配、无内部冲突。

云端 Sources 规定业务语义、数据模型和状态机；GitHub 正式源码用于确认实现事实。本分析不使用远程机旧规则副本替代云端 Sources。

## 2. 版本判定

现有软件已连续使用 V1.0.4 → V1.0.5 → V1.0.6 → V1.0.7 的补丁号递增方式，每次同步更新页面版本、CHANGELOG 与 Service Worker 缓存版本。因此本次下一正式软件版本按既有规则确定为 **PWA V1.0.8**，不把业务规格名称“V1.1 Frozen”直接当作软件版本。

当前 IndexedDB 为 V3，既有升级按整数顺延，因此本次安全迁移版本确定为 **V4**。当前 JSON 备份 schema 为 V2，本次新增对象后顺延为 **V3**，继续兼容 V1/V2 导入。

## 3. 逐项差异

| V1.1 Frozen 能力 | 当前状态 | V1.0.7 实现事实 | V1.0.8 所需工作 |
|---|---|---|---|
| 基金配置与版本化 | 未实现 | 无 `fund_profiles`，无显式基金代码、名称或净值来源 | 新增版本化配置、按日期取有效配置；禁止 `000852` |
| 基金单位净值层 | 未实现 | 无 `fund_nav_daily`、无正式基金NAV接口 | 新增高精度NAV存储、人工录入/修正、来源与revision；保留自动适配器边界，不伪造自动成功 |
| 三维状态 | 部分实现 | execution/revision 已独立；share confirmation 不存在 | 增加 `PENDING_NAV / CONFIRMED / MANUAL_CORRECTED / NOT_APPLICABLE` |
| T日投入即时记账 | 已实现 | 已执行后本金、资金来源、外部现金流、期数立即生效，不依赖NAV | 保持现状，并在同一操作中附加份额状态；不得让NAV缺失阻断记账 |
| T日份额延迟确认 | 未实现 | 无按交易日匹配NAV、无份额回填 | 严格只用原T日基金NAV；App打开及NAV写入后扫描回填 |
| 高精度份额计算 | 未实现 | 校准份额为微份额整数，但新增买入未计算份额 | 以整数/BigInt完成 `amountCent × 10^10 ÷ unitNavScaled` 的四舍五入，不用浮点持久化 |
| 当前递推份额 | 部分实现 | 仅保存人工校准总份额；止盈按比例产生新校准快照 | 以最新有效人工校准为锚，叠加其后有效已确认买入、扣减已确认赎回 |
| 人工校准锚点与纠偏 | 部分实现 | 市值和总份额必填并生成快照；缺少校准前递推份额和纠偏值 | 保存 anchor、derived-before、correction、anchorEffectiveAt；不删除旧确认记录 |
| 模拟基金市值 | 未实现 | 只有最近真实校准市值 | 新增 `derivedShares × latest valid fund NAV` 与四类可用状态；显式标为自动模拟 |
| 正式/模拟XIRR | 部分实现 | 正式XIRR使用最近校准市值+现金池 | 保持正式口径，新增共用外部现金流、仅终值不同的模拟XIRR并明确标签 |
| 净值人工修正 | 未实现 | 无NAV实体 | 原值/新值/原因/revision完整留痕；原子重算当日关联份额及后续派生值 |
| 份额确认审计 | 未实现 | 无 `share_confirmation_events` | 新增审计事件，不作为唯一计算事实源 |
| IndexedDB | 部分实现 | V3；已有 revision/settings 等，缺三类新store与记录扩展 | 升级V4，新增三store及索引，扩展交易/校准字段，旧数据不丢失 |
| V1.0.7数据迁移 | 未实现 | 旧交易无可靠基金代码/NAV/确认份额 | 已执行旧记录不编造份额，迁移为 `PENDING_NAV`；非已执行为 `NOT_APPLICABLE`；校准快照补锚点字段 |
| JSON备份恢复 | 部分实现 | V2完整遍历当时 stores，兼容V1/V2 | 升级V3并含新stores/扩展字段；继续兼容旧备份，缺失新store按空集合恢复 |
| 基金资料与NAV UI | 未实现 | 仅展示人工校准隐含净值，并明确官方NAV未接入 | 增加基金资料、NAV列表、人工录入/修正和修订历史 |
| 当前份额 UI | 部分实现 | 基金市值依据页只显示最近校准总份额 | 分开显示最近真实锚点、当前递推份额、待确认笔数 |
| 市值 UI | 未实现 | 只显示最近真实校准市值 | 明显区分“最近真实校准市值”和“当前自动模拟市值” |
| XIRR UI | 部分实现 | 单一“接管后策略XIRR” | 明确“正式接管后策略XIRR”与“模拟接管后策略XIRR” |
| 单笔三状态 UI | 部分实现 | 已有执行状态与修订状态 | 增加份额确认状态及NAV/确认份额信息 |
| 000852隔离 | 已实现基础 | 指数只进入 market/realtime/三因素链，未用于份额 | 加入跨域写入校验和自动化测试，基金NAV只进入基金估值链 |
| PWA离线 | 已实现基础 | V1.0.7缓存可离线启动 | 升级缓存版本并加入新核心文件，验证旧缓存升级无白屏 |

## 4. 自动基金净值数据源结论

当前源码没有明确、稳定、已验证的基金净值自动数据源；云端 Sources 也没有指定唯一供应商或 API。根据 Frozen，V1.0.8将实现：

- 可版本化的 `navSourceType` 与自动数据源适配器接口；
- 完整人工录入、人工修正和份额回填能力；
- 自动源不可用时明确显示限制，不伪造成功；
- 自动源失败不阻断本金即时记账和人工NAV流程。

由于没有明确生产基金代码，不能把 Sources 中的示例代码硬编码为用户配置。首次使用基金估值链前必须由用户在本机显式配置；该配置属于本地私人数据，不提交仓库。

## 5. 迁移策略

1. V3 → V4仅新增 stores、索引和默认字段，不删除或重建私人业务 store。
2. 历史已执行买入若没有可靠T日基金NAV，设置 `shareConfirmationStatus=PENDING_NAV`，`confirmedSharesMicro=null`；严禁用000852反推。
3. 未执行、待执行和硬上限停止记录设置 `NOT_APPLICABLE`。
4. 旧校准快照以已有 `totalSharesMicro` 补充 `anchorSharesMicro` 和 `anchorEffectiveAt`；无法在升级事务中可靠求得的校准前递推/纠偏值保持空，不伪造。
5. 当前递推只从最近有效校准锚点之后计算，锚点前未确认旧记录不影响当前份额，但仍保留供后续审计或人工补NAV。
6. V1/V2 JSON备份继续可导入；新对象缺失时建立空集合并运行同一兼容迁移。

## 6. 冲突与门禁结论

- Frozen冲突：无。
- 业务语义无法唯一实现：无。
- 需要停止开发的版本冲突：无。
- 下一软件版本：依据既有仓库规则确定为 V1.0.8。
- 数据库版本：依据当前V3整数迁移规则确定为V4。
- 自动NAV供应商：未指定；按规范实现适配器结构和完整人工流程，不宣称自动源已可用。
- 基金代码：必须由用户显式配置，不能硬编码示例值。

结论：差异分析完成，可直接进入开发阶段，无需二次确认。
