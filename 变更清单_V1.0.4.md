# PWA-REMOTE-002 变更清单｜V1.0.4

## 新增

- `market-data-core.js`：行情校验、合并、指标、北京时间、交易日和新鲜度纯逻辑。
- `tests/market-data-core.test.js`：行情通道、校验、日历与状态机测试。
- V1.0.4 执行报告、测试结果、变更清单、待确认事项和云端回传文件。

## 修改

- `index.html`：版本号、更新按钮、状态字段、IndexedDB行情适配、建议归属日与延迟降级说明。
- `market-provider.js`：三级行情通道、直连解析、落库、防回退和来源标识。
- `strategy-core.js`：行情不满足应有交易日时只使用基础规则。
- `sw.js`：V1.0.4缓存与真实网络/本地缓存来源区分。
- `tests/strategy-core.test.js`、`tests/check-project.js`：新增回归断言并更新版本基线。
- `README.md`、`README_GITHUB.md`、`DATA_SOURCES.md`、`DEPLOY_CHECKLIST.md`：同步 V1.0.4 运行与验收口径。

## 未修改

- Frozen 金额档位、仓位警戒、年度硬上限、现金池优先等业务规则。
- 私人投资数据字段、已有 IndexedDB 数据库版本和导入导出覆盖范围。
- GitHub Actions 行情更新与 Pages 部署工作流。

