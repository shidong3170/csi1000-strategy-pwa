# 数据源与部署口径｜GitHub版

## A股交易日历
- 权威依据：上交所年度正式休市安排；
- 深交所可用于交叉核验；
- 年度日历固化为本地JSON；
- 无可靠年度日历时，不猜测未来交易日。

## 中证1000市场数据
- 指数身份：000852；
- 实时观察源：东方财富实时/准实时接口（`MANUAL_DIRECT_REALTIME`）；仅展示当前点位、涨跌、涨跌幅和行情时间；
- 正式策略源：东方财富历史日K；
- 手机手动更新时优先直接读取公开日K（`MANUAL_DIRECT`）；
- 直连失败时读取 GitHub Pages 同源静态JSON（`GITHUB_AUTO`）；
- 网络通道均失败时读取设备 IndexedDB 行情缓存（`LOCAL_CACHE`）；
- IndexedDB中的行情按交易日保存，以日期为主键，收盘点位采用 `1e-4` 固定精度整数；
- 网络行情请求12秒未完成时视为失败并进入下一通道；
- GitHub Actions定时抓取并保存为 `data/csi1000-history.json`；
- 三因素全部在手机本地计算。
- 实时对象与完整日K对象分别保存于运行时；实时值不写入日K缓存，不参与回撤、近3年位置、MA200或100/150/200/300元判断。

## 数据新鲜度
- 交易日开盘前及盘中：上一实际交易日为最新完整收盘，状态为 `VALID_USING_LAST_COMPLETE_CLOSE`，建议有效；
- 交易日15:00～18:30：状态为 `WAITING_TODAY_CLOSE`，保留最近完整收盘建议；
- 交易日18:30后取得当日收盘：状态为 `VALID`，用于下一实际交易日建议；
- 18:30后仍缺当日收盘：状态为 `MARKET_DATA_DELAYED`，基础规则继续有效，新的市场加码暂缓；
- 相差1～3个交易日：延迟，不参与市场加码；
- 4～7个交易日：行情偏旧；
- >7个交易日：`MARKET_DATA_STALE`；
- 无法确认可靠交易日历：状态未知；
- 北京时间交易日15:00后，当天成为待确认的最新完整收盘日；18:30前缺失只显示等待，18:30后缺失才判定延迟。所有判断固定使用 `Asia/Shanghai`，不随设备所在时区改变。

## 隐私
GitHub只保存公开市场数据和程序，不保存个人投资数据。
