# PWA-REMOTE-004 变更清单｜V1.0.6

## 新增

- 中证1000实时/准实时行情独立适配器与观察卡片。
- `VALID`、`VALID_USING_LAST_COMPLETE_CLOSE`、`WAITING_TODAY_CLOSE`、`MARKET_DATA_DELAYED`、`MARKET_DATA_STALE` 五类策略行情状态。
- V1.0.6 测试矩阵、执行报告、待确认事项和云端回传材料。

## 修改

- `market-data-core.js`：北京时间阶段判断、完整日K过滤、实时响应校验和状态机。
- `market-provider.js`：实时行情与完整日K独立请求、独立对象、独立失败处理。
- `index.html`：拆分“实时市场”和“策略计算基准”，首页显示各自口径。
- `sw.js`：缓存版本升级为 V1.0.6，外部实时/日K请求改为网络直连，避免 Service Worker 返回旧行情。
- `tests/market-data-core.test.js`、`tests/check-project.js`：新增时间状态、双路异常、版本与页面断言。
- `README.md`、`README_GITHUB.md`、`DATA_SOURCES.md`、`DEPLOY_CHECKLIST.md`：同步 V1.0.6 口径。

## 未修改

- Frozen 金额档位、目标仓位、18%/19%警戒、年度硬上限、年度增量、现金池、XIRR、期限、止盈、补录、未执行和人工校准规则。
- 日K主数据来源与 GitHub 自动行情兜底。
- IndexedDB 版本、业务 store 与个人数据边界。
